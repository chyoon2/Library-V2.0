@{
  Layout = "_Layout";
}

@model Library.Models.Author;

<h2>Author Details</h2>
<hr />
<h3>@Html.DisplayNameFor(model => model.AuthorName): @Html.DisplayFor(model => model.AuthorName)</h3>

@if(@Model.Books.Count == 0)
{
  <p>This Author does not have any Books</p>
}
else
{
  <h4>Books written by the author:</h4>
  <ul>
  @foreach(var join in Model.Books)
  {
    <li>@join.Book.BookName</li>
  }
  </ul>
}

<p>@Html.ActionLink("Back to Author index", "Index")</p>
<p>@Html.ActionLink("Edit Link doesnt work sorry", "Edit", new { id = Model.AuthorId })</p>
<p>@Html.ActionLink("Delete Author this link doesnt work", "Delete", new { id = Model.AuthorId })</p>